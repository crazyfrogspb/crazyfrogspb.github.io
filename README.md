# Varim ML Blog

Автоматический блог на Jekyll, который синхронизирует статьи Telegraph из канала [@varim_ml](https://t.me/varim_ml) и публикует их на GitHub Pages.

## Возможности

- 🔄 **Автоматическая синхронизация** статей Telegraph из Telegram канала
- 🏷️ **Фильтрация по тегам** на основе хэштегов из постов
- 📊 **Сортировка** по дате и количеству просмотров
- 📱 **Адаптивный дизайн** для всех устройств
- 🖼️ **Автоматическое скачивание изображений** из Telegraph
- 🔗 **Ссылки на оригиналы** Telegraph и Telegram постов
- 🌙 **Поддержка темной темы**

## Структура проекта

```
├── _posts/                 # Jekyll посты
├── _layouts/              # Шаблоны страниц
├── _includes/             # Переиспользуемые компоненты
├── assets/
│   ├── css/              # Стили
│   ├── js/               # JavaScript для фильтрации
│   ├── images/           # Изображения из Telegraph
│   └── posts_index.json  # Индекс постов для фильтрации
├── tags/                 # Страницы тегов
├── scripts/
│   └── sync_telegraph.py # Скрипт синхронизации
└── .github/workflows/    # GitHub Actions
```

## Настройка

### 1. Настройка Telegram API

1. Получите API credentials на [my.telegram.org](https://my.telegram.org)
2. Добавьте secrets в GitHub репозиторий:
   - `TELEGRAM_API_ID`
   - `TELEGRAM_API_HASH`

### 2. Настройка GitHub Pages

1. В настройках репозитория включите GitHub Pages
2. Выберите источник: "GitHub Actions"

### 3. Локальная разработка

```bash
# Установка системных зависимостей (Ubuntu/Debian)
sudo apt update
sudo apt install ruby-full ruby-dev build-essential zlib1g-dev

# Установка Ruby зависимостей
bundle install

# Создание Python виртуального окружения
mkvirtualenv varim_ml

# Установка Python зависимостей
workon varim_ml
pip install -r requirements.txt

# Запуск Jekyll локально
bundle exec jekyll serve --host 0.0.0.0 --port 4000

# Ручная синхронизация (требует настройки API)
workon varim_ml
cd scripts
python sync_telegraph.py
```

## Использование

### Автоматическая синхронизация

- Запускается каждый день в 6:00 UTC через GitHub Actions
- Проверяет новые посты в канале @varim_ml
- Скачивает контент Telegraph статей
- Создает Jekyll посты с метаданными
- Обновляет индекс для фильтрации
- Публикует изменения на GitHub Pages

### Ручная синхронизация

Можно запустить синхронизацию вручную через GitHub Actions:

1. Перейдите в раздел "Actions" репозитория
2. Выберите "Manual Telegraph Sync"
3. Нажмите "Run workflow"

Опции:
- **Force re-sync**: Игнорировать кэш и обработать все посты заново
- **Limit messages**: Ограничить количество обрабатываемых сообщений

### Создание новых постов

Для создания постов напрямую в Markdown:

1. Создайте файл в `_posts/` с именем `YYYY-MM-DD-название.md`
2. Добавьте front matter:

```yaml
---
layout: post
title: "Заголовок поста"
date: 2024-01-01 12:00:00 +0300
tags: [машинное-обучение, python]
views: 0
excerpt: "Краткое описание поста"
---

Содержимое поста в Markdown...
```

## Фильтрация и сортировка

На главной странице доступны:

- **Сортировка**:
  - По дате (новые/старые)
  - По просмотрам (больше/меньше)

- **Фильтрация по тегам**:
  - Клик по тегу добавляет/убирает его из фильтра
  - Можно выбрать несколько тегов одновременно
  - Кнопка "Очистить фильтры" сбрасывает выбор

## Технические детали

### Обработка Telegraph статей

1. Извлечение ссылок из сообщений Telegram
2. Скачивание HTML контента
3. Обработка изображений (скачивание и локальное сохранение)
4. Конвертация HTML в Markdown
5. Извлечение метаданных (просмотры, заголовок)
6. Создание Jekyll поста с front matter

### Извлечение тегов

Теги извлекаются из хэштегов в Telegram постах:
- `#MachineLearning` → `machinelearning`
- `#Python` → `python`
- Приводятся к нижнему регистру
- Убираются дубликаты

### Кэширование

Скрипт ведет кэш обработанных URL в `.processed_urls.json` для избежания повторной обработки.

## Разработка

### Добавление новых функций

1. Для изменения внешнего вида редактируйте `assets/css/main.scss`
2. Для изменения логики фильтрации редактируйте `assets/js/posts-filter.js`
3. Для изменения логики синхронизации редактируйте `scripts/sync_telegraph.py`

### Тестирование

```bash
# Проверка синтаксиса Jekyll
bundle exec jekyll build

# Локальный сервер с автообновлением
bundle exec jekyll serve --livereload

# Проверка Python кода
python -m py_compile scripts/sync_telegraph.py
```

## Лицензия

MIT License

## Поддержка

При возникновении проблем создайте issue в репозитории или обратитесь к автору.
